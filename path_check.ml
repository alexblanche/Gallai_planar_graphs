(* Checking properties: path, decomposition *)

(* Colors are 0,...,n-1 *)

(* Check that a color col is a path *)
let is_a_path (cg : colored_graph) (col : color) =
  false


(* Check that the coloring of the colored graph cg is a path-decomposition *)
let is_a_path_decomposition (cg : colored_graph) =
  false